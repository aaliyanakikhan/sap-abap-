" we will use the standard table in internal table 
also we will use the types keyword for the template building & then we will use the data to fetche the data keyword 
When we declare the internal table the syntax is data : t_internaltablename type table of ty_tablename 
in above note we have used the 'type table of'  if we will remove this then it will turn in to work area & we know work area is only compatible to hold the 1 field While the internal table can hold the multiple field
"the internal table can be created with header or without header 
recommend to create without header
To declare that the table is standard there is two syntax
1. data  t_internaltablename type table of ty_tablename with header line.
2. data t_emp1 type standard table of ty_emp with header line.

header line --- is same as the work are that can store the single record one by one its present at the top of the internal table 
and the internal table is located in the below part of the header table 
& it have the rows & colums to store multiple data 

we understand in simple terms that the header is present on the top of body & the below is callled body that we can also call the internal tabl;e 

now if we are trying to fetche the database directly from the db it will present directly in internal table body 
but if we manually inserting the data then  we will firt put the data in header then in the internal table

APPEND IS UED TO INSERT THE NEW RECORD AT THE END OF THE INTERNAL TABLE.

SY-TABIX -- is the system field that is used to  refer the index position name of the internal table 
when a  specific record is inserted the record start form 1, 2 , 3 as goes on.

also go in utikities and choose the new editor juts make sure 
for the fomnts stting choose layout ---- setting ---- fonts.


" there 3 ways to clear the internal table .
1. clear  --can be used in var , wa , int.table 
2. refresh  A.(also refer to boddy) , used only in int.table.
            B. it will clear record but doesnt deallocate the mmemory (means if we have 100 record it will clear the recod only & does not cleatr the memory occupancy) 
3. free (refer to body). can be used in int/table only 
        it will clear the memory As well as the occupancy of the data in memory.
        cut shorrt it will take the int.table in to initial state.


" 2 ways of copy data 
1. append 
2. = operator 
3.append line /



report zinternal_table.

 types : begin of ty_emp,
         empno type i,
        ename(20) type c,
        empdesig(25) type c,
   end of ty_emp.


data : t_emp1 type  table of  ty_emp with header line. " in thios code we are try to declare the internal table with the header of the table 
  
clear t_emp1.' clear default header data.
 t_emp1-empno = 4." here the data is copy to header after that we will insert into the table by using append 
t_emp1-ename = 'raju'.
t_emp1-empdesig = 'manager'.

append t_emp1.  " append will copy the data from the header to internal table till end.

clear t_emp1. " we recommend to clear by our self a good practice - reste it will clear automatically 
" when ever we insert the new data the old data will automatically clear but we still use it to make the code clear

" now new entry for the updating the record the old record will now be inserted to internal table form the work area now new entry will come 

 t_emp1-empno = 5.
t_emp1-ename = 'krishna'.
t_emp1-empdesig = 'director'.



clear t_emp1.
 t_emp1-empno = 5.
t_emp1-ename = 'krishna'.
t_emp1-empdesig = 'director'.
append t_emp1

" now we will print the value.
 write :/ t_emp1-empno , t_emp1-ename , t_emp1-empdesig. " now in this statement the system will going to print the current work area value since the write statement will be only printing the work area 

" if we want to print the whole table then we will use the loop 
" we will use the loop in loop backend -- the data from the body is copied to work area line by line start form the 1 record 
the data come form the internal table to the work area one by one & start printing it

write /' data from the internal table'.
loop  at t_emp1.
write :/  t_emp1-empno , t_emp1-ename , t_emp1-empdesig , sy-tabix ." the system field sy-tabix is used to print the index value of the field data that is present in the internal table  " here the loop start & it will print the data 
endloop.

" loop backend --- data from internal table ---> work area " it will iterate till the data is not printed 
" append ---data from database -------> work area -----> internal table.

" now we will use the index no & print the value as per our requirement 

uline.
" the loop at specific position.
loop  at t_emp1  from 2 to 4 . " this will skip the 1 record & print the rest
write :/  t_emp1-empno , t_emp1-ename , t_emp1-empdesig , sy-tabix ." the system field sy-tabix is used to print the index value of the field data that is present in the internal table  " here the loop start & it will print the data 
endloop.


"to print the specific position only. using the where statement - note we can use the field name to apply the condision.

loop  at t_emp1  where empdesig = 'manager'
or empdesig = 'Manager'
or empdesig = 'MANAGER' . " data is case sensitive.
write :/  t_emp1-empno , t_emp1-ename , t_emp1-empdesig , sy-tabix ." the system field sy-tabix is used to print the index value of the field data that is present in the internal table  " here the loop start & it will print the data 
endloop.

ULINE.
" now i want to check the no of records form the internal table. 
"using the describe table keyword we can dispaly the reord or analyze the no. iof records

DESCRIBE TABLE t_emp1. " this will count the value form the internal table & store the value is system- field  called sy-tfill.

write :/ ' no of the records in t_emp1 is .... ', sy-tfill.

 " now we will clear internal table -- note to clear internal table body we use [].
clear t_emp1[]. the symbol represent the body." now we will chcek again the internal table values.

DESCRIBE TABLE t_emp1. " now this will show 0 in place of the values.


"now we will declare new internal table without header & also we ill copy the fields form the earlier intenrla table.


data t_emp2 type table of ty_emp.  this will make the new internal table witgout header=.

uline .
describe table t_emp2.

write :/ 'no of records in t_emp2 is ,,,,',sy-tfill. " it will show 0.


Append lines of t_emp1 to t_emp2. " this will help to  copy all data from old internal table to new internal table.


uline.
describe table t-emp2. " now it will show 4.
write :/ 'no of records in t_eemo2 is ,,', sy-tfill. 


" now we will make one more it and then use the 'free' refresh keyword with it

data  t_emp3 type table of ty_emp.
uline.

describe table t_emp3.
write :/ 'statement to descrive the emp3 '.


refresh t_emp2. " this will clear the data

uline.
describe table t_emp2.
write :' no of rows emp2 after refresh'.

free t-emp3.
describe table t_emp2.
write :' no of rows emp2 after free'." this clear data.










 



 



