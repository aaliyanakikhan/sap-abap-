* this program focus on the structure --- " A stucture is a user define data type unlike other data type like int char these are standard data type but the stucture is the user define data type.
*Define stucture -- the stucture is the user define data type which have the collection of the fields
 *we use the stucture when we want to use the inter related data.EG emp data.
*/ the fields in the structure is store one after the other like 1 b , b2 , b3 like this its helpfull to fetch the data & use it in the code. each is use the 39 bytes \\
* now if we want to access the  fields in the stucture then we have to use the ~ to connect the stucture name & the field name  EG----   Stucturename~fieldname 
 * a structure field can only hold only one data at a time , if we want multiple data in one field we can consider the internal table for the execution 
EG--- emp id = 1 empname = raju  empdesignation = 'manager'.
*now we will write the program to decalre & use the stucture fields

*&---------------------------------------------------------------------*
*& Report ZSTRUCTURE_PRAC
*&---------------------------------------------------------------------*
*&
*&---------------------------------------------------------------------*
REPORT ZSTRUCTURE_PRAC.

data : begin of emp,
       empno type i,
       empname(20) type c,
       empdesig(20) type c,
   end of emp.
*
*if i would have skip the below line the  values of the field  will not print since the flow of the program.
***when we check the program we have first mention the write statement at
***  that time the value is still 0 but after that we have assign some
***  values then print it will work like this
***
   write : / 'begin of the stucture '.
   write : / emp-empno , emp-empname , emp-empdesig.

   emp-empno = 4.
   emp-empname = 'raju'.
   emp-empdesig = ' manager'.
*After adding this below statement at this point we have value then we are printing it .
   write : / ' emp stucture...'.
   write : / emp-empno , emp-empname , emp-empdesig.


############output 

begin of the stucture 
0     --   --
 emp stucture 
0 raju mamager 


 

 
 
